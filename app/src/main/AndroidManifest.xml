<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.queryapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.StudentAnswerList"></activity>
        <activity android:name=".views.TeacherReply" />
        <activity android:name=".views.TeacherHome" />
        <activity android:name=".views.StudentHome" />
        <activity android:name=".views.TeacherRegister" />
        <activity android:name=".views.StudentRegister" />
        <activity android:name=".views.StudentSignIn" />
        <activity android:name=".views.TeacherSignIn" />
        <activity android:name=".views.IntroActivity" />
        <activity android:name=".views.LoginMode" />
        <activity android:name=".views.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>